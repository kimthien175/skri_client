import 'package:flutter/material.dart';

class GameToolbar extends StatelessWidget {
  const GameToolbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(width: 800, height: 100, color: Colors.white);
  }
}