import 'package:flutter/material.dart';

class GameChat extends StatelessWidget {
  const GameChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 300, height: 100, color: Colors.white
    );
  }
}