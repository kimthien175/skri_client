import 'package:flutter/material.dart';

class GamePlayers extends StatelessWidget {
  const GamePlayers({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(width: 200, height: 100,color: Colors.white);
  }
}